#include <iostream>
using namespace std;

int main()
{
    int *ptr;

    // Dynamically allocate memory
    ptr = new int;

    *ptr = 25;

    cout << "Value: " << *ptr << endl;

    // Release dynamically allocated memory
    delete ptr;

    return 0;
}
